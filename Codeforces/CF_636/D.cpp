/**
* Copyright(c)
* Author : tiketiskte
*/
#include <bits/stdc++.h>
#define IOS {ios::sync_with_stdio(false);cin.tie(0);}
#define ll long long
#define INF 0x3f3f3f3f

using namespace std;

const int maxn = 2e5 + 5; 
int t, n, k;
int a[maxn];
int main()
{
   IOS
   cin >> t;
   while(t--) {
       cin >> n >> k;
       for(int i = 1; i <= n; i++) {
           cin >> a[i];
       }
       
   }
   system("pause");
   return 0;
}
