/**
* Copyright(c)
* Author : tiketiskte
*/
#include <bits/stdc++.h>
#define IOS {ios::sync_with_stdio(false);cin.tie(0);}

using namespace std;
int N;
char ch;
int main()
{
   IOS
   cin >> N >> ch;
   for(int i = 1; i <= N; i++)
   {
       cout << ch << " ";
       int n = (n + 1) * 2; 
       int m = n / 2;
   }
   system("pause");
   return 0;
}
